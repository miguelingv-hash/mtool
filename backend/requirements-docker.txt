# Dependencias mínimas para ejecutar la app SII Consulta.
# Usado por backend/Dockerfile para producir una imagen ligera y reproducible.
# El requirements.txt grande de la raíz contiene el entorno completo de
# desarrollo y no debe usarse en despliegue.

# Web
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
starlette==0.41.3

# Datos
pydantic==2.10.4
python-dotenv==1.0.1
motor==3.6.0
pymongo==4.9.2

# SII / SOAP / mTLS
zeep==4.3.2
lxml==5.3.0
requests==2.32.3
cryptography==44.0.0
