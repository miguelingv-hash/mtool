# Dependencias mínimas para ejecutar `descargar_sii.py` desde tu equipo
# (sin necesidad de instalar el backend completo).
#
# Instalación:
#   pip install -r scripts/requirements-cli.txt
#
# Las versiones se mantienen alineadas con backend/requirements.txt.

pymongo>=4.6,<5
zeep>=4.2,<5
lxml>=4.9
cryptography>=42
requests>=2.31
fastapi>=0.110
pydantic>=2.5
motor>=3.3
python-dotenv>=1.0
